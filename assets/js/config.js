/* ============================================================
   config.js — настройки развёртывания.
   Этот файл специально маленький и лежит отдельно: его правят
   руками перед публикацией, остальной код трогать не нужно.
   ============================================================ */
window.FHH_CONFIG = {

  /* Где панель управления вообще доступна.
     'local'  — только когда сайт открыт локально (localhost / 127.0.0.1 /
                двойной клик по файлу). На опубликованном домене панель
                не открывается ничем: ни точкой в подвале, ни Ctrl+Shift+A,
                ни адресом #/admin.  ← значение по умолчанию
     'always' — доступна везде (только если вы понимаете, что проверка
                пароля происходит в браузере и надёжной защитой не является).
     'remote' — панель открывается и на опубликованном сайте, но ТОЛЬКО по
                адресу #/admin или Ctrl+Shift+A (точки в подвале нет), и любая
                правка остаётся в браузере, пока её не опубликуют токеном
                (см. раздел github ниже и РУКОВОДСТВО, п. 09).
     'off'    — выключена полностью. */
  admin: 'remote',

  /* Опубликованное содержимое сайта. Файл готовится в панели управления
     («Данные» → «Файл для публикации») и коммитится в репозиторий.
     Пустая строка — не загружать ничего, работать только на демо-каталоге. */
  dataUrl: 'data/site.json',

  /* Куда панель отправляет витрину при публикации прямо из браузера.
     Работает только в режиме admin: 'remote'. Токен здесь НЕ хранится и
     никогда не попадает в файлы — его вводят в панели, и он живёт до
     закрытия вкладки. */
  github: {
    owner: 'fantasyhhorse',
    repo: 'fantasyhhorse.github.io',
    branch: 'main',
    path: 'data/site.json'
  }
};
